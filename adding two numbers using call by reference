#include <stdio.h>

void add(int *x, int *y) {
    *x = *x + *y;
}

int main() {
    int num1,num2;
    printf("Enter two numbers to add:\n");
    scanf("%d %d", &num1, &num2);
    add(&num1, &num2);
    printf("The sum is %d\n", num1);
    return 0;
}
