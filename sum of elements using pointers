#include <stdio.h>

int main() {
   int i,j,arr[] = { 2, 3, 4, 5,6};
   int n = sizeof(arr) / sizeof(int);

   int sum = 0;
   int *ptr = arr;

   for ( i = 0; i < n; i++) {
      sum += *ptr;
      ptr++;
   }

   printf("The sum of all elements in the array is %d\n", sum);
   return 0;
}
