#include<stdio.h>
int main()
{
	int num,rev_number=0,remainder;
	printf("enter the number to reverse:");
	scanf("%d",&num);
	while(num!=0)
	{
		remainder=num%10;
		rev_number=rev_number*10+remainder;
		num=num/10;
	}
	printf("the rev_number is %d",rev_number);
	return 0;
}
